<div class="mx-auto max-w-3xl px-4 py-12 sm:px-6 lg:px-8">
  <h1 class="text-3xl font-bold tracking-tight-headline text-foreground">{{ page.title }}</h1>
  <div class="mt-6 prose prose-sm prose-invert max-w-none text-visible-muted">{{ page.content }}</div>
</div>
